!!!
%html
  %head
    %title BDD Demo
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags

  %body.container
    %nav.navbar
      .navbar-inner
        .container
          .brand Behavior Driven Development Demo
    - if(flash.present?)
      - flash.each do |key, value|
        .alert{class: key}= value
    = yield
